## Quick Start — Visual Demo

```bash
python3 demo_visual.py
```

Opens a browser at `http://localhost:8844` with the visual solitaire game.
Type commands in the terminal — both screens stay in sync.
Type `agent play` to watch the AI take over.